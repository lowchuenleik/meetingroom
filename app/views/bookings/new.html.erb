<div class='container'>
	<div class="row">
	  <div class="col-md-4">
	    <h1>New Booking</h1>
	    <%= render partial: 'form', locals: { booking: @booking, client: @client, venue: @venue } %>
	  </div>  
	  <div class="col-md-8">
	    <!-- <%= add_weekly_calendar(@bookings) %> -->
	    <div id='calendar'></div>
	  </div>
	</div>
</div>