<div class="container">
  <p id="notice"><%= notice %></p>

  <h1 style="padding-top: 3rem ;">Venues</h1>

  <br>

  <!--
  <table class = "table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Price</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @venues.each do |venue| %>
        <tr>
          <td><%= venue.name %></td>
          <td><%= venue.price %></td>
          <td><%= link_to 'Show', venue %></td>
          <td><%= link_to 'Edit', edit_venue_path(venue) %></td>
          <td><%= link_to 'Destroy', venue, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  -->

  <div class="card card-info p-3 text-left">
    <blockquote class="card-blockquote">
      <p>Welcome to our booking page, we hope you find somewhere you may like.</p>
        <small>
          <cite title="Source Title">Hang Team</cite>
        </small>
    </blockquote>
  </div>
  <br>
  <div class="row justify-content-lg-center">
    
      <div class="card-deck">
        <% @venues.each do |venue| %>
          <div class="col-md-4">
            <div class="card" id="Venue">
              <img class="card-img-top" src="http://placehold.it/350x250" alt="Card image cap">
              <div class="card-block">
                <h2 class="float-right">£<%= venue.price %></h2>
                <h4 class="card-title"><%= venue.name %></h4>
                <p class="font-italic lead card-text">Mandarin Oriental</p>
              </div>
              <div class="card-footer">
                  <span><%= link_to "Book now", "#", class: "btn btn-primary btn-md" %></span>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    
  </div>

  <br>



  <%= link_to 'New Venue', new_venue_path %>
</div>